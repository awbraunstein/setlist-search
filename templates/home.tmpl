{{define "sample_queries"}}
<div class="sample-queries">
    <h2>Sample Queries</h2>
    {{range .}}
	<div class="query"><a href="/search?query={{.}}">{{.}}</a></div>
    {{end}}
</div>
{{end}}

<!doctype html>
<html>
    <head>
	<title>Setlist Searcher</title>
    </head>
    <body>
	<div>
	    <h1>Search Phish Setlists:</h1>
	</div>
	<div>
	    {{template "searchbox"}}
	    {{template "sample_queries" .SampleQueries}}
	</div>
    </body>
</html>
